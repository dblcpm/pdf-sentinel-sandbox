name: Security Vulnerability Scanning

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  security-scan:
    runs-on: ubuntu-latest
    
    permissions:
      contents: read  # Minimal permissions for security
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install pip-audit
        run: |
          python -m pip install --upgrade pip
          pip install pip-audit
      
      - name: Run pip-audit vulnerability scan on requirements.in
        run: |
          # Scan requirements.in directly to avoid compilation overhead
          # Ignore CVE-2026-0994: protobuf fix requires >=7.x but streamlit requires <7
          # This is a known limitation until streamlit supports protobuf 7.x
          pip-audit -r requirements.in --ignore-vuln CVE-2026-0994
